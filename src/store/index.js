import { createStore } from 'vuex'

import country from "./country";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    country,
  }
})
